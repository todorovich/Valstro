This project requires Docker, Gradle, and Java 17.

The following docker image must be running on localhost

```docker run -p 3000:3000 clonardo/socketio-backend```

To run the application use gradle 

```gradlew run```